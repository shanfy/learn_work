<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="test" >
    	<insert id="add" parameterType="com.yang.entity.User" >
    		insert into mybatis_user(id,username,userpass,sex) values(#{id},#{username},#{userpass},#{sex})
    	</insert>
    </mapper>